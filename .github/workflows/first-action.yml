name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    name: my first job
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello"
      - name: Print Good bye
        run: echo "Done - Bye!"
